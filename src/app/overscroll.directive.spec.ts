import { OverscrollDirective } from './overscroll.directive';

describe('OverscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new OverscrollDirective();
    expect(directive).toBeTruthy();
  });
});
